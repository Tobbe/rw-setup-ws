{
  "name": "rw-setup-ws",
  "version": "0.0.1",
  "description": "Setup WebSockets in RedwoodJS",
  "main": "./dist/cli.js",
  "bin": "./dist/cli.js",
  "scripts": {
    "build": "rm -fr ./dist && pkgroll",
    "cleannpx": "rm -fr ./npx && mkdir -p ./npx/bin && mkdir ./npx/templates",
    "copypkgfiles": "yarn cleannpx && cp README.md ./npx/ && cp package.json ./npx/",
    "copybin": "cp ./dist/*.js ./npx/bin",
    "copytemplates": "cp ./templates/* ./npx/templates/",
    "copyforrelease": "yarn copypkgfiles && yarn copybin && yarn copytemplates",
    "release:patch": "yarn build && npm version patch && yarn copyforrelease && cd npx && npm publish",
    "release:minor": "yarn build && npm version minor && yarn copyforrelease && cd npx && npm publish",
    "release:major": "yarn build && npm version major && yarn copyforrelease && cd npx && npm publish"
  },
  "keywords": [
    "redwood",
    "websockets",
    "redwoodjs",
    "websocket",
    "realtime"
  ],
  "author": "Tobbe Lundberg",
  "license": "MIT",
  "devDependencies": {
    "@redwoodjs/cli": "^4.0.0",
    "pkgroll": "^1.9.0"
  },
  "peerDependencies": {
    "@redwoodjs/cli": "^4.0.0"
  },
  "dependencies": {
    "@types/findup-sync": "^4.0.2",
    "@types/yargs": "^17.0.22",
    "chalk": "^5.2.0",
    "execa": "^7.0.0",
    "findup-sync": "^5.0.0",
    "listr2": "^5.0.7",
    "yargs": "^17.7.1"
  }
}
